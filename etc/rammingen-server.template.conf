{
    "database_url": "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}",
    // Backup archive directory
    "storage_path": "${ARCHIVE_STORAGE_DIR}",
    "bind_addr": "0.0.0.0:${SERVER_PORT}",
}
